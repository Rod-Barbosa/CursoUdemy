// Escreva uma funcao chamada ePaisagem que 
// recebe dois argumentos, largura e altura
// da uma imagem (number)
// retorne true se a imagem estiver no modo paisagem

//const ePaisagem (largura, altura) => largura>altura?true:false;
//faltou o igual

function ePaisagem(largura, altura){
    return largura>altura; //? true : false; ---> retornando a condicao
                            // vc ja consegue o boolean
}

const ePaisagem2 = (largura, altura) => largura > altura;